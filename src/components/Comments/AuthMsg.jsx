import { AuthContainer, AuthTitle, AuthButton } from './Comments.styled';

const AuthMsg = () => {
  return (
    <AuthContainer>
      <AuthTitle>Hello!</AuthTitle>
      <AuthButton>Log In</AuthButton>
    </AuthContainer>
  );
};
export default AuthMsg;
